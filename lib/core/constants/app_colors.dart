import 'package:flutter/material.dart';

class AppColors {
  static const white = Colors.white;
  static const black = Colors.black;
  static const orange = Colors.orange;
  static const red = Colors.red;

  static const grey = Colors.grey;
  static final grey10 = Colors.grey.withAlpha((0.1*255).round());
  static final grey20 = Colors.grey.withAlpha((0.2*255).round());
  static final grey30 = Colors.grey.withAlpha((0.3*255).round());
  static final grey70 = Colors.grey.withAlpha((0.7*255).round());

  // Якщо треба повністю прозорий
  static const transparent = Colors.transparent;

}